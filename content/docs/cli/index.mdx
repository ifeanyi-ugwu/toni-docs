---
title: CLI
description: Scaffolding projects and resources, and the watching development loop.
---

`toni-cli` does three things: creates a project that compiles, generates a wired module/controller/service triple, and runs a watch loop that rebuilds and restarts on save. The dev loop can also hold the listening socket across restarts, so a rebuild does not refuse connections.

<Cards>
  <Card title="Installation" href="/docs/cli/installation" description="Installing the CLI, and the scaffolding-only build." />
  <Card title="Commands" href="/docs/cli/commands" description="toni new, toni generate resource, and every flag toni dev passes to cargo." />
</Cards>
